# 🚀 Деплой на Render.com (100% Бесплатно)

**Render.com** - идеальный выбор для FREE хостинга FastAPI приложения с PostgreSQL и Redis!

## ⚡ Преимущества Render.com

✅ **Полностью бесплатно:**
- Веб-сервис на Python (0.1 CPU, 512MB RAM)
- PostgreSQL база (до 90 дней)
- Redis инстанс
- 750 часов в месяц бесплатного веб-сервиса

✅ **Простота:**
- Один клик деплоя из GitHub
- Автоматические обновления на каждый push
- Встроенные логи и мониторинг

✅ **Надёжность:**
- Хороший апдайм (99.5%)
- Зарезервированные ресурсы
- Поддержка PostgreSQL 15

## 📋 Требования

1. GitHub аккаунт (с запушенным проектом)
2. Render.com аккаунт (свяжите с GitHub)
3. Telegram Bot Token от @BotFather
4. 15 минут времени

## 🔧 Пошаговая инструкция

### Шаг 1: Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **Sign Up**
3. Выберите "Sign up with GitHub"
4. Авторизуйтесь и дайте Render доступ к вашим репозиториям

### Шаг 2: Создание PostgreSQL базы

1. На дашборде Render нажмите **New +** → **PostgreSQL**
2. Заполните:
   - **Name:** `underpeople-db`
   - **Database:** `underpeople_db`
   - **User:** `underpeople`
   - **Plan:** Free
3. Нажмите **Create Database**
4. ⏳ Дождитесь инициализации (2-3 минуты)
5. Скопируйте **Internal Database URL** (понадобится позже)

### Шаг 3: Создание Redis инстанса

1. Нажмите **New +** → **Redis**
2. Заполните:
   - **Name:** `underpeople-redis`
   - **Plan:** Free
3. Нажмите **Create Redis**
4. ⏳ Дождитесь инициализации
5. Скопируйте **Redis URL**

### Шаг 4: Деплой FastAPI приложения

1. Нажмите **New +** → **Web Service**
2. Выберите репозиторий `under-people-platform`
3. Заполните:
   - **Name:** `under-people-api`
   - **Runtime:** Python 3.11
   - **Build Command:** 
     ```bash
     pip install -r backend/requirements.txt
     ```
   - **Start Command:**
     ```bash
     cd backend && gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:10000
     ```
   - **Plan:** Free

4. Нажмите **Advanced** и добавьте Environment Variables:

| Key | Value | Примечание |
|-----|-------|-----------|
| `PYTHON_VERSION` | `3.11.6` | |
| `DATABASE_URL` | *(скопировать из БД)* | Internal Database URL |
| `REDIS_URL` | *(скопировать из Redis)* | Redis Connection String |
| `TELEGRAM_BOT_TOKEN` | *(от @BotFather)* | ⚠️ Генерируется отдельно |
| `SECRET_KEY` | *(сгенерировать)* | `openssl rand -hex 32` |
| `ENVIRONMENT` | `production` | |
| `DEBUG` | `false` | |
| `ALLOWED_ORIGINS` | `https://your-frontend-domain.vercel.app,https://underpeople.club` | |

5. Нажмите **Create Web Service**
6. ⏳ Дождитесь деплоя (2-3 минуты)

### Шаг 5: Проверка деплоя

После успешного деплоя вы получите URL вида:
```
https://under-people-api.onrender.com
```

Проверьте API:
```bash
curl https://under-people-api.onrender.com/docs
```

Должна открыться документация FastAPI (Swagger)!

## 🔑 Генерация SECRET_KEY

В терминале выполните:
```bash
# Windows (PowerShell)
python -c "import secrets; print(secrets.token_hex(32))"

# macOS/Linux
openssl rand -hex 32
```

Скопируйте результат в переменную `SECRET_KEY` на Render.

## 🤖 Получение Telegram Bot Token

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Выполните `/newbot`
3. Дайте боту имя (например, `under_people_bot`)
4. Скопируйте Token API вида `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`
5. Вставьте в переменную `TELEGRAM_BOT_TOKEN` на Render

## 🔗 Подключение к Frontend

После успешного деплоя API на Render.com:

### На Vercel (Frontend)

Добавьте переменную окружения:
```
NEXT_PUBLIC_API_URL=https://under-people-api.onrender.com
```

### Локально

Обновите `.env.local`:
```
NEXT_PUBLIC_API_URL=https://under-people-api.onrender.com
```

## ⚠️ Важные особенности бесплатного плана Render

1. **Спящий режим:** Web Service "спит" после 15 минут неактивности
   - Первый запрос может быть медленнее на 30 сек
   - Решение: кликните на сервис в Render дашборде чтобы пробудить вручную

2. **Лимиты:**
   - 750 часов в месяц (≈ 31 день работы)
   - 512MB RAM на веб-сервис
   - 1GB дата транспорт в месяц

3. **Удаление неиспользуемых БД:**
   - PostgreSQL удаляется после 90 дней неактивности
   - Redis удаляется после 30 дней неактивности

## 🚨 Если что-то не работает

### Проблема: API возвращает ошибку 502/503

**Решение:**
1. Откройте **Logs** на страничке Web Service
2. Проверьте ошибки (обычно проблема с DATABASE_URL или миграциями)
3. Пересоздайте Web Service если нужно

### Проблема: PostgreSQL не инициализируется

**Решение:**
1. Удалите старую БД
2. Создайте новую (Render сама создаст таблицы при подключении)
3. Обновите DATABASE_URL

### Проблема: Redis не подключается

**Решение:**
1. Убедитесь что REDIS_URL скопирован правильно (без лишних пробелов)
2. Редис должен быть в том же регионе что и Web Service
3. Проверьте логи: `redis-cli ping`

## 📊 Мониторинг и логи

Все логи доступны в Render дашборде:
- **Logs** - реальные логи приложения
- **Metrics** - CPU, память, сетевой трафик
- **Events** - история деплоев

## 🔄 Обновление кода

Просто запушьте изменения на GitHub:
```bash
git add .
git commit -m "Обновление API"
git push origin main
```

Render автоматически переразвернёт приложение! 🚀

## 💡 Альтернативные варианты

Если Render вам не подходит, есть другие бесплатные варианты:

### Railway.app ($5/месяц кредитов)
- Более быстрый от природы
- Удобный интерфейс
- [Инструкция в RAILWAY_DEPLOYMENT.md](./RAILWAY_DEPLOYMENT.md)

### PythonAnywhere (бесплатно)
- Очень медленно
- Более сложный деплой
- Рекомендуется только как запасной вариант

## ✅ Чек-лист успешного деплоя

- [ ] GitHub аккаунт создан и проект залит
- [ ] Render аккаунт создан и связан с GitHub
- [ ] PostgreSQL база создана (Render)
- [ ] Redis инстанс создан (Render)
- [ ] Web Service создан и развёрнут
- [ ] Environment переменные установлены
- [ ] API доступен по HTTPS
- [ ] Frontend подключен к API
- [ ] Telegram Bot Token установлен
- [ ] Всё работает! 🎉

---

**Готово!** Ваш API теперь работает в облаке 24/7 абсолютно бесплатно!
