{
  "name": "under-people-platform",
  "version": "1.0.0",
  "description": "Under People Club monorepo - see frontend for Next.js app",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm start",
    "vercel-build": "cd frontend && npm install --legacy-peer-deps --include=dev && npm run build"
  },
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
