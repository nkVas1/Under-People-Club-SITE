# 🎯 ЧТО ДЕЛАТЬ ДАЛЬШЕ: ПОЛНАЯ ИНСТРУКЦИЯ

## ✅ Что уже сделано

1. ✅ **render.yaml обновлен** - конфигурация правильная с правильными путями
2. ✅ **RENDER_FIX.md создан** - полное руководство по исправлению ошибки
3. ✅ **Изменения запушены на GitHub** - все файлы уже на сервере

## 🔄 ШАГ ЗА ШАГОМ: Как пересоздать деплой на Render

### 1️⃣ Убедитесь, что локально всё правильно

**Откройте терминал в папке `under-people-platform` и выполните:**

```powershell
# Windows PowerShell - проверить структуру
Test-Path ".\render.yaml"
Test-Path ".\backend\requirements.txt"
Test-Path ".\backend\app\main.py"

# Должны вывести: True, True, True
```

### 2️⃣ Обновите конфиг переменных окружения

**Файл `backend\.env` (создайте, если не существует):**

```env
# Когда будете создавать БД на Render, она даст вам этот URL
DATABASE_URL=postgresql://underpeople:PASSWORD@dpg-xyz.onrender.com/underpeople_db

# Получите от @BotFather в Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZ

# Сгенерируйте локально:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-32-characters-min

# Когда будете создавать Redis на Render
REDIS_URL=redis://default:PASSWORD@redis-xyz.onrender.com:6379

# Для CORS - разрешить фронтэнду
ALLOWED_ORIGINS=http://localhost:3000,https://your-vercel-domain.vercel.app
```

### 3️⃣ Откройте Render.com

**Перейдите на** [https://dashboard.render.com](https://dashboard.render.com)

### 4️⃣ Удалите старый деплой (опционально)

Если у вас уже есть `under-people-api` с ошибкой:

1. Откройте **Settings** сервиса
2. Прокрутите вниз до **Danger Zone**
3. Нажмите **Delete Web Service**
4. Подтвердите

### 5️⃣ Создайте PostgreSQL базу

1. На дашборде Render нажмите **+ New** → **PostgreSQL**
2. Заполните:
   - **Name:** `underpeople-postgres`
   - **Database:** `underpeople_db`
   - **User:** `underpeople`
   - **Password:** *(автоматически сгенерируется)*
   - **Region:** `Frankfurt` (или ваш регион)
   - **Plan:** `Free`

3. Нажмите **Create Database**
4. ⏳ Дождитесь инициализации (2-3 минуты)
5. **Когда готово**, откройте БД и найдите **Internal Database URL**
6. **Скопируйте** этот URL (понадобится для переменной окружения)

**Примерно выглядит так:**
```
postgresql://underpeople:xxx@dpg-1234.onrender.com/underpeople_db
```

### 6️⃣ Создайте Redis (опционально, но рекомендуется)

1. Нажмите **+ New** → **Redis**
2. Заполните:
   - **Name:** `underpeople-redis`
   - **Region:** Как у PostgreSQL
   - **Plan:** `Free`

3. Нажмите **Create**
4. ⏳ Дождитесь инициализации
5. **Скопируйте Redis URL**

### 7️⃣ Создайте новый Web Service

1. Нажмите **+ New** → **Web Service**
2. Выберите ваш GitHub репозиторий (нажмите **Connect** если нет)
3. Нажмите на репозиторий `Under-People-Club-SITE`

**Заполните данные:**

| Параметр | Значение |
|----------|----------|
| **Name** | `under-people-api` |
| **Region** | Как у БД |
| **Branch** | `main` |
| **Runtime** | `Python 3` (автоматически) |
| **Build Command** | *(оставить пусто - будет из render.yaml)* |
| **Start Command** | *(оставить пусто - будет из render.yaml)* |
| **Plan** | `Free` |

### 8️⃣ Добавьте переменные окружения

Нажмите **Environment** и добавьте каждую переменную:

```
DATABASE_URL = postgresql://underpeople:xxx@dpg-1234.onrender.com/underpeople_db
TELEGRAM_BOT_TOKEN = 1234567890:ABCDEFghijklmNOPqrstuvWXYZ
SECRET_KEY = your-secret-key-32-characters
REDIS_URL = redis://default:xxx@redis-abc.onrender.com:6379
ALLOWED_ORIGINS = http://localhost:3000,https://your-vercel.vercel.app
```

⚠️ **ВАЖНО:**
- Копируйте РОВНО то, что вы скопировали из БД/Redis
- Не добавляйте комментарии (только значение)
- Проверьте, что нет пробелов в начале/конце

### 9️⃣ Создайте сервис

Нажмите **Create Web Service**

## 📊 Смотрите логи деплоя

После нажатия **Create Web Service**:

1. Откроется страница сервиса
2. **Перейдите на вкладку Logs**
3. **Дождитесь** появления одного из сообщений:

✅ **Правильно (успех):**
```
==> Build successful 🎉
==> Deploying...
[INFO] Starting gunicorn
[INFO] Application startup complete
```

❌ **Ошибка (неправильно):**
```
bash: line 1: cd: backend: No such file or directory
```

### Если вы видите ошибку:

**Проверьте:**

1. В корне проекта есть файл `render.yaml`?
   ```powershell
   Test-Path ".\render.yaml"  # Должно вывести True
   ```

2. `render.yaml` содержит:
   ```yaml
   buildCommand: "pip install -r backend/requirements.txt"
   startCommand: "cd backend && gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
   ```

3. На GitHub последний коммит содержит обновленный `render.yaml`:
   ```powershell
   git log --oneline | Select-Object -First 1
   # Должен показать: "Исправлены конфигурация Render деплоя..."
   ```

4. Если всё правильно, но ошибка остаётся - **Redeploy**:
   - Settings → **Redeploy latest commit**

## 🧪 Тестируем API после успешного деплоя

Когда деплой завершен, вы получите URL вроде:
```
https://under-people-api.onrender.com
```

**Проверьте:**

```powershell
# 1. Базовое здоровье API
curl "https://under-people-api.onrender.com/api/health"
# Должно вернуть: {"status":"ok"}

# 2. Документация API
# Откройте в браузере: https://under-people-api.onrender.com/docs
```

## 🌐 Обновите фронтэнд

### Если деплоим на Vercel (рекомендуется)

**Файл `frontend/.env.local`:**
```env
NEXT_PUBLIC_API_URL=https://under-people-api.onrender.com
```

**Затем:**
```bash
git add frontend/.env.local
git commit -m "Обновлены параметры API URL для Render / Update API URL for Render"
git push origin main
```

Vercel автоматически redeploy произойдет!

### Если localhost

Просто измените в `frontend/.env.local`:
```env
NEXT_PUBLIC_API_URL=https://under-people-api.onrender.com
```

И перезагрузите фронтэнд локально.

## 📚 Документация по Render

- **Полная инструкция:** Читайте [RENDER_FIX.md](RENDER_FIX.md)
- **Из-за ошибок:** Смотрите раздел "Если всё ещё не работает"
- **Логирование:** [Render Docs - Logs](https://render.com/docs/logs)

## 🎉 ГОТОВО!

После успешного деплоя:

1. ✅ API доступен по `https://under-people-api.onrender.com`
2. ✅ PostgreSQL база создана и инициализирована
3. ✅ Redis подключен для кэширования
4. ✅ Telegram Bot может авторизовывать пользователей
5. ✅ Фронтэнд может обращаться к API

## 🚀 Следующие шаги

1. **Настроить Telegram Bot:**
   - Создать бота через @BotFather
   - Установить Webhook URL: `https://under-people-api.onrender.com/api/telegram/webhook`
   - Получить Bot Token и добавить в TELEGRAM_BOT_TOKEN

2. **Настроить Vercel фронтэнд:**
   - Импортировать репозиторий на vercel.com
   - Root Directory: `frontend`
   - Добавить переменную окружения: `NEXT_PUBLIC_API_URL=...`

3. **Добавить custom домен (опционально):**
   - В Render: Settings → Custom Domain
   - Добавить DNS записи согласно инструкции

---

**Вопросы?** Читайте [RENDER_FIX.md](RENDER_FIX.md) - там полная пошаговая инструкция с примерами логов и решением проблем! 🚀
